# Patreon Reader Environment Configuration
# Copy this file to .env and configure your settings

# ===========================================
# SECURITY
# ===========================================
# API authentication token (leave empty to disable auth)
# Generate a secure token: openssl rand -hex 32
API_TOKEN=

# ===========================================
# PATREON AUTH
# ===========================================
# RECOMMENDED: Use session cookie (most reliable method)
# To get this:
# 1. Login to Patreon in your browser
# 2. Open Developer Tools (F12)
# 3. Go to Application > Cookies > patreon.com
# 4. Copy the value of "session_id" cookie
PATREON_SESSION=

# Alternative: Patreon credentials for automated login
# Note: This may be blocked by Patreon's anti-bot detection
PATREON_EMAIL=your-email@example.com
PATREON_PASSWORD=your-password

# ===========================================
# SYNC SETTINGS
# ===========================================
# Background sync interval in hours (default: 2)
SYNC_INTERVAL_HOURS=2

# ===========================================
# SERVER SETTINGS
# ===========================================
# Port to expose (default: 8000)
PORT=8000

# Public-facing URL of the application
# Used by PWA to connect to the correct backend
# Leave empty for same-origin (local development)
# Example: https://patreon.example.com
APP_URL=

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Timezone for logs (default: UTC)
TZ=UTC

# ===========================================
# DIRECTORIES
# ===========================================
# Output directory for EPUB files
OUTPUT_DIR=./output

# Cache directory for downloaded content
CACHE_DIR=./cache
